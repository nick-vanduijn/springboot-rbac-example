# Spring Boot Configuration for RBAC API

# Application Info
spring.application.name=Spring RBAC API
server.port=8080

# Database Configuration (H2 for demonstration)
spring.datasource.url=jdbc:h2:mem:rbacdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (for development only)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Logging
logging.level.com.example.rbac=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT Configuration (externalized for production)
app.jwt.secret=4D6351665468576D5A7134743777217A25432A46294A404E635266556A586E32
app.jwt.expiration=86400000